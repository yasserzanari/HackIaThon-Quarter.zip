{
  "InstanceIds": ["i-0c837343e3ded4133"],
  "DocumentName": "AWS-RunShellScript",
  "Parameters": {
    "commands": [
      "export HOME=/root && apt-get install -y poppler-utils 2>&1 | tail -5",
      "which pdftoppm && pdftoppm -v 2>&1 || echo 'pdftoppm NOT FOUND'",
      "which libreoffice && libreoffice --version 2>&1 || echo 'LibreOffice NOT FOUND'",
      "ls -la /var/www/html/wp-content/uploads/pedagolens/slides/ 2>/dev/null || echo 'slides dir not found'"
    ]
  }
}
